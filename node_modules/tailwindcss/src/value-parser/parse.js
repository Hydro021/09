p jobbraÚj lap balraMásodpéldányA többi lap bezárásaJobbra lévő lapok bezárásaBalra lévő lapok bezárásaAz összes lap bezárása{NUM_TABS,plural, =1{Webhely némítása}other{Webhelyek némítása}}{NUM_TABS,plural, =1{Webhely némításának feloldása}other{Webhelyek némításának feloldása}}{NUM_TABS,plural, =1{Lap hozzáadása olvasási listához}other{Lapok hozzáadása olvasási listához}}{NUM_TABS,plural, =1{Lap hozzáadása csoporthoz}other{Lapok hozzáadása csoporthoz}}{NUM_TABS,plural, =1{Lap hozzáadása új csoporthoz}other{Lapok hozzáadása új csoporthoz}}Új csoportEltávolítás a csoportból{NUM_OTHER_TABS,plural, =0{„$1”}=1{„$1” és 1 másik lap}other{„$1” és # másik lap}}{NUM_TABS,plural, =1{Lap áthelyezése új ablakba}other{Lapok áthelyezése új ablakba}}{NUM_TABS,plural, =1{Lap áthelyezése másik ablakba}other{Lapok áthelyezése másik ablakba}}Webhely követéseWebhely követésének megszüntetéseItt találhatja lapjait{NUM_OPEN_TABS,plural, =1{# megnyitott lap, nyomja meg a lapsor be-/kikapcsolásához}other{# megnyitott lap, nyomja meg a lapsor be-/kikapcsolásához}}Lap bezárásaA lapok kereséséhez kattintson ide.Keresés a lapokonLap bezárva$1 lapot találtunk$1 lap található a következőre: „$2”Lap megnyitásaLegutóbb bezárt lapLegutóbb bezárt lapcsoportHang és videóMostanában bezárt$1 lapVáltás a nemrégiben bezárt elemek közöttA „Mostanában bezárt” szakasz kibontásaA „Mostanában bezárt” szakasz összecsukásaÖsszes lapOrganize TabsCsoport létrehozásaYou can organize your tabs anytime{NUM_PAGES,plural, =0{$1}=1{$1 és további 1 lap}other{$1 és további # lap}}Lapcsoport címeCsoport mentéseCsoport mentésének visszavonásaÚj lap a csoportbanCsoportosítás megszüntetéseCsoport bezárásaCsoport törléseCsoport áthelyezése új ablakbaCsoport megnyitása új ablakbanNevezze el a csoportotBöngészőablak megnyitása&Link másolása&QR-kód létrehozásaFrissítés befejezéseÚjraindítás a frissítéshezRendelkezésre áll az új Chrome$1 – Képernyő-megosztási kérésSzeretné, ha a(z) $1 megosztaná az Ön képernyőjét?Szeretné, ha a(z) $1 megosztaná az Ön képernyőjét és a hangkimenetét?A(z) $1 megosztja az Ön képernyőjét.A(z) $1 megosztja a képernyőt és a hangot.A(z) $1 megoszt egy ablakot.A(z) $1 megosztja a Chrome egy lapját.A(z) $1 megosztja a Chrome egy lapját és a hangot.ElrejtésForrás módosításaTanúsítvány kiválasztásaVálasszon tanúsítványt a $1 hitelesítéséhezSegítse mérnökeinket az összeomlás kivizsgálásában és kijavításában. Ha lehet, adja meg a pontos lépéseket. Nem létezik túl apró részlet!
Visszajelzés megszakított lapról.
Ha többé nem akar hozzáférni ehhez a programhoz, el kell távolítania azt a Vezérlőpulton a(z) $1 segítségével.

Elindítja a következőt: $1?Böngésződet a szülőd kezeliBeállítottuk kezdőoldalát.Ezt a beállítást a(z) „$1” bővítmény kényszeríti ki.Ezt a beállítást egy bővítmény kényszeríti ki.A rendszergazda javaslatát fogadja el ennél a beállításnál.Rendszergazdája egy meghatározott értéket javasol ehhez a beállításhoz.Ez a beállítás nem áll rendelkezésre a fiókjában.Ezt a beállítást szülő kezeli.Nem a Chrome Internetes áruházból származik.Harmadik fél telepítette.Vállalati házirend által telepítve.Telepítve az ezt nélkülözni nem tudó bővítmény(ek) miatt.Ez a bővítmény rosszindulatú programot tartalmaz.Ez a bővítmény komoly biztonsági rést tartalmaz.A bővítmény sérti a Chrome Webáruház irányelvét.A Chrome letiltotta. Ez a bővítmény nem biztonságos.További információ a Biztonságos Böngészés fokozott védelemmel funkcióról.További információ a hozzáférési kérelmekről.Ez a művelet visszaállítja a kezdőoldalt, az új lap oldalt, a keresőmotort és a rögzített lapokat. Kikapcsol továbbá minden bővítményt, és törli az ideiglenes adatokat, például a cookie-kat. Könyvjelzői, el�e: name,
        before: value.slice(parenthesesOpenPos + 1, next),
      }
      pos = next

      if (name === 'url' && code !== singleQuote && code !== doubleQuote) {
        next -= 1
        do {
          escape = false
          next = value.indexOf(')', next + 1)
          if (~next) {
            escapePos = next
            while (value.charCodeAt(escapePos - 1) === backslash) {
              escapePos -= 1
              escape = !escape
            }
          } else {
            value += ')'
            next = value.length - 1
            token.unclosed = true
          }
        } while (escape)
        // Whitespaces before closed
        whitespacePos = next
        do {
          whitespacePos -= 1
          code = value.charCodeAt(whitespacePos)
        } while (code <= 32)
        if (parenthesesOpenPos < whitespacePos) {
          if (pos !== whitespacePos + 1) {
            token.nodes = [
              {
                type: 'word',
                sourceIndex: pos,
                sourceEndIndex: whitespacePos + 1,
                value: value.slice(pos, whitespacePos + 1),
              },
            ]
          } else {
            token.nodes = []
          }
          if (token.unclosed && whitespacePos + 1 !== next) {
            token.after = ''
            token.nodes.push({
              type: 'space',
              sourceIndex: whitespacePos + 1,
              sourceEndIndex: next,
              value: value.slice(whitespacePos + 1, next),
            })
          } else {
            token.after = value.slice(whitespacePos + 1, next)
            token.sourceEndIndex = next
          }
        } else {
          token.after = ''
          token.nodes = []
        }
        pos = next + 1
        token.sourceEndIndex = token.unclosed ? next : pos
        code = value.charCodeAt(pos)
        tokens.push(token)
      } else {
        balanced += 1
        token.after = ''
        token.sourceEndIndex = pos + 1
        tokens.push(token)
        stack.push(token)
        tokens = token.nodes = []
        parent = token
      }
      name = ''

      // Close parentheses
    } else if (closeParentheses === code && balanced) {
      pos += 1
      code = value.charCodeAt(pos)

      parent.after = after
      parent.sourceEndIndex += after.length
      after = ''
      balanced -= 1
      stack[stack.length - 1].sourceEndIndex = pos
      stack.pop()
      parent = stack[balanced]
      tokens = parent.nodes

      // Words
    } else {
      next = pos
      do {
        if (code === backslash) {
          next += 1
        }
        next += 1
        code = value.charCodeAt(next)
      } while (
        next < max &&
        !(
          code <= 32 ||
          code === singleQuote ||
          code === doubleQuote ||
          code === comma ||
          code === colon ||
          code === slash ||
          code === openParentheses ||
          (code === star && parent && parent.type === 'function' && true) ||
          (code === slash && parent.type === 'function' && true) ||
          (code === closeParentheses && balanced)
        )
      )
      token = value.slice(pos, next)

      if (openParentheses === code) {
        name = token
      } else if (
        (uLower === token.charCodeAt(0) || uUpper === token.charCodeAt(0)) &&
        plus === token.charCodeAt(1) &&
        isUnicodeRange.test(token.slice(2))
      ) {
        tokens.push({
          type: 'unicode-range',
          sourceIndex: pos,
          sourceEndIndex: next,
          value: token,
        })
      } else {
        tokens.push({
          type: 'word',
          sourceIndex: pos,
          sourceEndIndex: next,
          value: token,
        })
      }

      pos = next
    }
  }

  for (pos = stack.length - 1; pos; pos -= 1) {
    stack[pos].unclosed = true
    stack[pos].sourceEndIndex = value.length
  }

  return stack[0].nodes
}
